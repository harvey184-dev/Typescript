const path = require('path');

module.exports = {
    entry: './src/index.ts',
    devtool: 'inline-source-map',
    module: {
        rules: [
            {
                test: /\.ts$/,
                use: 'ts-loader',
                exclude: /node_modules/,
            },
        ],
    },
    resolve: {
        extensions: ['.ts', '.js'],
    },
    output: {
        filename: 'bundle.js',
        path: path.resolve(__dirname, 'dist'),
        // Add the following line to use ES6 module syntax
        // This tells webpack to use ES6 module syntax in the generated bundle
        // rather than CommonJS syntax
        environment: {
            module: true,
        },
    },
};
